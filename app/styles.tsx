import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    IndexView: {
        flex: 1,
        backgroundColor: '#fff',
    },
    container: {
        padding: 8,
        flexDirection: 'row',
        alignItems: 'center',
    },
    image: {
        width: 100,
        height: 100,
        marginRight: 32,
    },
    name: {
        fontWeight: 'bold',
        fontSize: 32,
    }
})

export default styles